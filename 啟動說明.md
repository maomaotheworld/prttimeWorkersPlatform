# 工讀生管理系統 - 啟動說明

## 系統架構

- **後端**: Node.js + Express (端口: 3005)
- **前端**: Vue3 + Vite (端口: 5175 - **固定不可更改**)

## ⚠️ 重要端口配置

- **前端必須運行在 PORT 5175**
- 如果 5175 被占用，請先關閉占用該端口的程序
- 前端配置中已設定 `strictPort: true` 確保端口固定

## 啟動步驟

### 🔧 前置準備

確保已安裝 Node.js 和 npm

### 📦 安裝依賴 (僅首次需要)

**後端依賴:**

```powershell
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\backend
npm install
```

**前端依賴:**

```powershell
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\frontend
npm install
```

### 🚀 啟動服務

#### 方法一: 分別啟動 (推薦)

**1. 啟動後端 API (端口3005):**

```powershell
# 開啟第一個 PowerShell 視窗
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\backend
node index.js
```

**2. 啟動前端服務 (端口5175):**

```powershell
# 開啟第二個 PowerShell 視窗
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\frontend
npm run dev
```

#### 方法二: 使用背景執行

```powershell
# 啟動後端 (背景執行)
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\backend
Start-Process powershell -ArgumentList "-NoExit", "-Command", "node index.js"

# 啟動前端
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\frontend
npm run dev
```

### 🌐 存取網址

#### 本機存取:

- **前端介面**: http://localhost:5175
- **後端API**: http://localhost:3005

#### 手機/平板存取:

- **前端介面**: http://192.168.1.117:5175
- (需確保裝置在同一Wi-Fi網路)

### 🔍 確認服務狀態

**檢查後端是否運行:**

```powershell
Invoke-WebRequest -Uri "http://localhost:3005/api/workers" -Method GET
```

預期回應: `{"success":true,"data":[],"message":"工讀生列表獲取成功"}`

**檢查端口使用狀況:**

```powershell
netstat -an | findstr ":3005"  # 檢查後端
netstat -an | findstr ":5175"  # 檢查前端
```

### 🛑 停止服務

**停止前端:**
在前端終端按 `Ctrl + C`

**停止後端:**
在後端終端按 `Ctrl + C`

**強制停止所有Node程序:**

```powershell
taskkill /f /im node.exe
```

### ⚠️ 常見問題

#### 1. 端口被佔用

```powershell
# 查看哪個程序佔用端口
netstat -ano | findstr ":3005"
netstat -ano | findstr ":5175"

# 強制終止特定PID
taskkill /f /pid [PID號碼]
```

#### 2. 前端無法連接後端

- 確認後端服務已啟動
- 檢查防火牆設定
- 確認端口配置正確

#### 3. 手機無法存取

- 確認電腦和手機在同一Wi-Fi網路
- 確認電腦防火牆允許相關端口
- 檢查IP位址是否正確

### 📱 行動裝置存取設定

**獲取電腦IP位址:**

```powershell
ipconfig | findstr "IPv4"
```

**在手機瀏覽器輸入:**

```
http://[電腦IP位址]:5175
```

### 🔧 開發模式

**後端開發模式 (自動重啟):**

```powershell
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\backend
npm run dev
```

**前端開發模式 (已預設):**

```powershell
cd c:\Users\evelyn.pan\Downloads\GIT\prttimeWorkersPlatform\frontend
npm run dev
```

### 📋 系統功能

- ✅ 工讀生管理 (新增/編輯/刪除)
- ✅ Excel匯入功能 (僅電腦版)
- ✅ 批次薪資設定
- ✅ 組別管理
- ✅ 工時記錄
- ✅ 薪資管理
- ✅ 響應式手機介面
- ✅ 區域網路存取

---

**最後更新**: 2026年1月21日
**版本**: 1.0.0
